# 匿名10人対戦の再構築
状態: done
タスク種別: 機能追加

## 背景と範囲
利用者がWorkの未push作業消失を受けて再構築を指示。最新origin/mainから専用worktreeで再実装する。前回のテスト結果を今回の証拠として流用しない。途中コミットもpushして保存する。

## 受け入れ条件
- [x] ログイン不要の作成・参加、最大10人、11人目拒否。
- [x] 入力・位置・接触を同期し、最下位確定時に終了。再接続・退出・ホスト移譲。
- [x] 全コースGPと毎回コース選択できるフリー。
- [x] FastAPI/Mangum Lambda、DynamoDB単独DB、AppSync、CloudFront/S3/API GatewayをCDKで構成。
- [x] Devスタンダード正本とAPI/CDK実装由来文書を自動生成。再生成一致・差分拒否を検証。
- [x] 厳格な型検査、API/物理/競合/認可テスト、10ブラウザと既存回帰を検証。
- [x] レポート、push、PR、CI、受入コメントを完了。

## 実装・検証計画
APIとDynamoDB状態機械→ブラウザ連携→CDK/文書生成→単体/ブラウザ検証。READMEとREQ/ARC/DES/OPSを更新する。匿名tokenの非公開、順位の競合、生成物の再現性をセルフレビューする。実AWSデプロイと計測はアカウント未指定につき別途。AWS配備済みとは報告しない。

## 完了の証拠
PR #23へ受入コメントを投稿した後にdoneへ移動。コードSHA `782652076442d243206bfb925965a8b326082df3` のCI run `34002755934` と `34002755928` が成功。バックエンド13件、単体108件、オンラインE2E4件、既存E2E39件成功。端末重複による意図的skipはオンライン2件・既存12件。最終CIの7画面を目視確認。詳細は `reports/working/20260906-0945-rebuild-multiplayer.md`。
